{
  "name": "@carbon-silicon/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types for Carbon-Silicon Organization",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
