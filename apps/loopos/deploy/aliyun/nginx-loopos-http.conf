# Include these locations inside the existing csi-org.com HTTP server block.
location = /loopos {
    return 301 https://csi-org.com$request_uri;
}

location ^~ /loopos/ {
    return 301 https://csi-org.com$request_uri;
}
