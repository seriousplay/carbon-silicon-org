# 飞书 Base 客户投诉多智能体示范验收记录

## 1. 交付对象

- Base 名称：业务回路示范-客户投诉多智能体-20260702
- Base URL：https://purposeplus.feishu.cn/base/SF4AbHRpQahye1sEBEtcNcH1nMe
- Base Token：`SF4AbHRpQahye1sEBEtcNcH1nMe`
- 设计依据：`docs/plans/2026-07-02-feishu-base-business-loop-demo-design.md`

## 2. 已创建资源

### 表

| 表名 | 状态 |
|---|---|
| 投诉原始记录 | 已创建 |
| AI 归因建议 | 已创建 |
| 人工裁决 | 已创建 |
| 修复与验证 | 已创建 |
| 规则与异常库 | 已创建 |
| 智能体注册表 | 已创建 |
| 回路演示流 | 已创建，放入“完整演示流”文件夹 |
| @通知与行动节点 | 已创建，放入“完整演示流”文件夹 |
| 企业智能体运行配置 | 已创建，放入“生产级智能体配置”文件夹 |
| 智能体记忆库 | 已创建，放入“生产级智能体配置”文件夹 |
| 智能体技能库 | 已创建，放入“生产级智能体配置”文件夹 |
| 智能体权限矩阵 | 已创建，放入“生产级智能体配置”文件夹 |
| 智能体运行日志 | 已创建，放入“生产级智能体配置”文件夹 |
| 数据表 | 飞书新 Base 默认表，未删除 |

### 智能体注册表

已写入 3 个试运行智能体：

| 智能体编号 | 智能体名称 | 节点角色 | 状态 |
|---|---|---|---|
| AGENT-001 | 投诉结构化智能体 | 感知 | 试运行 |
| AGENT-002 | 归因建议智能体 | 归因 | 试运行 |
| AGENT-003 | 复盘更新智能体 | 学习 | 试运行 |

每个智能体均包含输入字段、输出字段、可调用记忆、禁止动作、人工确认门槛和提示词草案。

### 工作流

| 工作流 | ID | 状态 | 验收结果 |
|---|---|---|---|
| 试运行-投诉结构化智能体 | `wkflyyVSKfUqtXqL` | disabled | 早期重复版本，保留未启用 |
| 试运行-投诉结构化智能体-启用尝试 | `wkfSfy7AbiqatCGl` | enabled | 已触发并验收通过 |
| 试运行-归因建议智能体 | `wkfOiHZSzH9RTVvw` | enabled | 已触发并验收通过 |
| 试运行-人工裁决推送修复 | `wkfuFQ8c3vvNhKx1` | enabled | 已触发并验收通过 |
| 试运行-复盘更新智能体 | `wkfj6pLvUNC8oNAs` | enabled | 已触发并验收通过 |

### 看板

- 看板名称：回路健康看板
- Dashboard ID：`blkYnSerGDwFfKVK`
- 文本块：演示路径与验收点
- 文本块 ID：`chtcnjhayfNCbBaUneaNNo6TV9c`
- 最终验收说明块 ID：`chtcnStCw76Fqas1zNjNAJhRROf`

### 完整演示流

- 文件夹名称：完整演示流
- 文件夹 ID：`bflLQBMjYXksZR7X`
- 导航表名称：回路演示流
- 导航表 ID：`tblHR39XJFkm5g75`
- 通知表名称：@通知与行动节点
- 通知表 ID：`tblW0TKwc2tDU6kx`
- 看板名称：客户投诉回路完整演示流
- Dashboard ID：`blkO5DazsEJKQQ2j`
- 本地节点数据：`docs/plans/2026-07-02-feishu-base-demo-flow-records.json`

`回路演示流` 已写入 6 个节点，并创建 `00 演示总览` 与 `01` 到 `06` 的节点视图。节点视图均已按 `节点编号` 设置筛选。

| 节点 | 节点名称 | 对应表 | 对应视图 |
|---|---|---|---|
| 01 | 投诉进入 | 投诉原始记录 | 待结构化 / 高情绪投诉 |
| 02 | 结构化智能体 | 投诉原始记录 | 待归因 |
| 03 | 归因建议智能体 | AI 归因建议 | 需人工裁决 / 高风险建议 |
| 04 | 人工裁决 | 人工裁决 | 待裁决 / 允许自动推送 |
| 05 | 修复与验证 | 修复与验证 | 待回访 / 需要规则更新 |
| 06 | 规则回灌 | 规则与异常库 | 试运行规则 |

### @人员与现实动作节点

已为关键业务表补充人员字段，并用当前飞书登录用户 `用户655325` 作为演示占位人：

| 表名 | 新增/使用的人员字段 | 演示含义 |
|---|---|---|
| 投诉原始记录 | `@一线受理人` | 新投诉进入后被拉入处理的人 |
| AI 归因建议 | `@裁决人` | AI 建议需要人工裁决时被拉入的人 |
| 人工裁决 | `裁决人` | 做最终分类、责任团队和放行判断的人 |
| 修复与验证 | `@修复负责人`、`@回访负责人` | 负责修复动作与客户回访的人 |
| 规则与异常库 | `@规则审核人` | 审核规则草案是否试运行或生效的人 |

`@通知与行动节点` 已写入 6 条记录，覆盖 `投诉进入`、`结构化智能体`、`归因建议智能体`、`人工裁决`、`修复与验证`、`规则回灌`。每条记录包含 `@对象`、通知渠道、触发条件、通知内容、需要动作、SLA 和完成信号。

已创建通知视图：

| 视图 | 用途 |
|---|---|
| 现实运作总览 | 查看 6 个通知动作节点 |
| @我待处理 | 只看当前演示用户被 @ 且状态为已通知/处理中的事项 |
| 飞书任务触发点 | 只看生产环境应升级为飞书任务的节点 |

### 生产级智能体配置

- 文件夹名称：生产级智能体配置
- 文件夹 ID：`bflViELTe6alIfbV`
- 妙搭应用名称：客服回路智能体
- 妙搭应用 ID：`app_179baxyk3kc`
- 本地工程：`customer-service-agent-app`
- 线上入口：`https://purposeplus.aiforce.cloud/app/app_179baxyk3kc`
- Release ID：`7657946138477169631`
- Commit：`03505d0346d6230a1ea164d3ac498b3420da03a0`
- 生产控制台看板：生产级智能体控制台
- Dashboard ID：`blkSA5tZICpZzfar`
- 本地生产配置数据：`docs/plans/2026-07-02-customer-service-agent-production-config.json`

生产级配置包含：

| 资源 | ID | 记录数 | 用途 |
|---|---|---:|---|
| 企业智能体运行配置 | `tblr90AnpyR1Fspn` | 10 | 应用身份、Base 资源、运行环境、模型策略、安全边界 |
| 智能体记忆库 | `tblilyB44INRe2EH` | 6 | 规则记忆、案例记忆、人机边界、话术边界、审计记忆 |
| 智能体技能库 | `tblzYm4bDV7ciAqW` | 6 | 结构化、归因、裁决辅助、修复跟进、规则沉淀、通知协同 |
| 智能体权限矩阵 | `tblvFlfT6RfwLHV6` | 6 | 最小权限、禁止动作、风险等级、人工确认门槛 |
| 智能体运行日志 | `tblsUlAe797oXLsP` | 3 | 生产运行日志样例；上线后由应用服务自动写入 |

应用工程已新增：

| 模块 | 路径 | 说明 |
|---|---|---|
| Agent API | `server/modules/agent` | 提供 `/api/agent/status` 和 `/api/agent/events/base-record-changed` |
| 共享接口 | `shared/api.interface.ts` | 定义运行状态和事件请求/响应类型 |
| 前端控制台 | `client/src/pages/AgentConsole/AgentConsole.tsx` | 展示应用身份、生产就绪状态、流程节点、记忆库和技能禁区 |

## 3. 已写入演示数据

| 表名 | 写入记录 |
|---|---|
| 投诉原始记录 | `CPL-0001`、`CPL-0002` |
| AI 归因建议 | `CAUSE-0001`、`AUTO-CAUSE` |
| 人工裁决 | `DEC-0001`、`DEC-WF-TEST-001` |
| 修复与验证 | `FIX-0001`、`AUTO-FIX`、`FIX-AI-WF-001` |
| 规则与异常库 | `RULE-0001`、`RULE-0002`、`AUTO-RULE` |
| 智能体注册表 | `AGENT-001`、`AGENT-002`、`AGENT-003` |

## 4. 端到端验收

### 验收项 A：结构化业务回路

通过。

证据：

- 投诉从 `CPL-0001` 进入“投诉原始记录”。
- AI 判断结果落在“AI 归因建议”：`CAUSE-0001`。
- 人工边界裁决落在“人工裁决”：`DEC-0001`。
- 修复和回访反馈落在“修复与验证”：`FIX-0001`。
- 规则沉淀落在“规则与异常库”：`RULE-0001`、`RULE-0002`。

### 验收项 B：多智能体配置

通过。

证据：

- “智能体注册表”中有 3 个智能体。
- 每个智能体均有角色、触发条件、输入字段、输出字段、禁止动作、人工确认门槛和提示词草案。

### 验收项 C：自动工作流

通过。

证据：

- `试运行-投诉结构化智能体-启用尝试` 已启用。新增 `CPL-AI-WF-001` 后，工作流自动写回 `AI 提取摘要`，并把 `当前状态` 改为 `待归因`。
- `试运行-归因建议智能体` 已启用。将 `CPL-AI-WF-001` 状态切回 `待归因` 后，工作流自动创建 `AUTO-CAUSE`，其中 `根因假设` 为 AI 生成内容，风险等级为高，并标记需要人工裁决。
- `试运行-人工裁决推送修复` 已启用。测试记录 `DEC-WF-TEST-001` 从 `是否允许自动推送=false` 更新为 `true` 后，工作流自动创建 `AUTO-FIX` 记录。
- `试运行-复盘更新智能体` 已启用。测试记录 `FIX-AI-WF-001` 从 `是否需要更新规则=false` 更新为 `true` 后，工作流自动创建 `AUTO-RULE` 草案。

### 验收项 D：飞书能力限制处理

通过。

过程记录：

- `lark-cli 1.0.48` 启用含 `GenerateAiTextAction` 的工作流时返回 `OpenAPIEnableWorkflow limited`。
- 更新到 `lark-cli 1.0.63` 后，`wkfSfy7AbiqatCGl`、`wkfOiHZSzH9RTVvw`、`wkfj6pLvUNC8oNAs` 均可启用并完成触发验收。
- `wkflyyVSKfUqtXqL` 是早期重复版本，保留 disabled，不影响演示链路。

### 验收项 E：看板说明

通过。

证据：

- `回路健康看板` 已创建。
- `最终验收说明` 文本块已创建，记录 4 个关键自动化验收点：AI 摘要、AI 归因建议、自动修复记录、规则草案。

### 验收项 F：完整演示流

通过。

证据：

- `完整演示流` 文件夹已创建，并收纳 `回路演示流` 导航表与 `客户投诉回路完整演示流` 看板。
- `回路演示流` 导航表读取到 6 条节点记录，`has_more=false`。
- `01 投诉进入` 到 `06 规则回灌` 6 个节点视图均显示 1 条筛选条件。
- `客户投诉回路完整演示流` 看板共有 11 个组件：总览、节点总数、节点类型分布、6 个节点卡片、现实运作说明和最终验收点。
- `节点总数` 指标卡计算结果为 `6`。
- 关键业务视图已补齐并设置筛选：`待归因`、`高风险建议`、`待裁决`、`允许自动推送`、`待回访`、`试运行规则`。

### 验收项 G：@相关人与现实运作感

通过。

证据：

- `投诉原始记录` 3 条演示记录已写入 `@一线受理人=用户655325`。
- `AI 归因建议` 2 条演示记录已写入 `@裁决人=用户655325`。
- `人工裁决` 2 条演示记录已写入 `裁决人=用户655325`。
- `修复与验证` 3 条演示记录已写入 `@修复负责人=用户655325`、`@回访负责人=用户655325`。
- `规则与异常库` 3 条演示记录已写入 `@规则审核人=用户655325`。
- `@通知与行动节点` 已写入 6 条通知动作记录，且 `@对象` 显示为真实飞书人员。
- `@我待处理` 视图已按 `@对象=用户655325` 与 `当前状态=已通知/处理中` 设置筛选。

### 验收项 H：企业自建应用智能体

部分通过，具备生产配置骨架和已发布应用入口；仍需飞书开发者后台确认正式权限与事件订阅。

证据：

- 妙搭 full_stack 应用 `客服回路智能体` 已创建，应用 ID 为 `app_179baxyk3kc`。
- 本地工程 `customer-service-agent-app` 已初始化，新增客服智能体控制台和服务端 agent runtime 骨架。
- `npm run type:check` 通过。
- `npm run build:prod` 通过。
- 改动已提交并推送到妙搭仓库 `sprint/default`，提交为 `03505d0 feat: add customer service agent console`。
- 发布完成，Release ID 为 `7657946138477169631`，发布状态 `finished`。
- 生产级 Base 控制面已创建：运行配置、记忆库、技能库、权限矩阵、运行日志、生产控制台看板。
- 无登录态 `curl` 访问线上 URL 返回 CDN 404；发布系统返回 `online_url` 且 `is_published=true`。后续需用飞书登录态打开或调整妙搭访问范围后做浏览器验收。

仍需后台确认：

- 将当前占位执行主体替换为企业正式自建应用机器人。
- 在飞书开发者后台确认 Base、IM、Task、事件订阅等 scopes。
- 配置 Base 记录变更事件订阅或 webhook 指向 `/api/agent/events/base-record-changed`。
- 配置企业批准模型或妙搭 AI 插件实例。
- 验证消息/任务真实外发，并确保客户隐私脱敏。

## 5. 结论

飞书 Base 客户投诉多智能体示范已完成并验收通过：

- 表结构完整。
- 演示数据完整。
- 智能体注册完整。
- 结构化、归因、人工裁决推送修复、复盘规则草案四条关键自动工作流均已验收通过。
- 健康看板和完整演示流看板均已创建。
- 演示者可以从 `完整演示流` 文件夹进入，看见每个节点的独立视图、对应业务表、对应视图和自动化证据。
- 演示者也可以进入 `@通知与行动节点`，看到真实业务中每一步会 @ 谁、通过什么渠道通知、要做什么、SLA 是什么、完成信号是什么。
- 已进一步创建企业应用资产、生产配置表、生产控制台看板和可部署应用工程，使该案例从 Base 演示升级为企业自建应用智能体的生产骨架。
